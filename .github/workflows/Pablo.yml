on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Unity
        uses: webbertakken/setup-unity@v2
        with:
          unityVersion: '2021.3.0f1'

      - name: List directories
        run: |
          ls -la /home/runner  # Example of listing files in a typical location
          ls -la /opt  # Another common location for Unity installs